import { describe, it, expect } from 'vitest'

// Placeholder proving the unit-test runner is wired up. Real tests arrive with
// their features (see specs/06-testing-quality.md).
describe('scaffold sanity', () => {
  it('runs the test runner', () => {
    expect(true).toBe(true)
  })
})
