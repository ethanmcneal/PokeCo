// Nuxt UI theme tokens. A red primary nods to the Poké Ball; neutral slate keeps
// the chrome calm so type colors carry the visual weight (see TypeBadge).
export default defineAppConfig({
  ui: {
    colors: {
      primary: 'red',
      neutral: 'slate',
    },
  },
})
